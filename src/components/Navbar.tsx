import React from 'react'

function Navbar() {
  return (
    <nav className="uk-navbar-container" id="navbar">
        <a href="#">Home</a>
        <a href="#">Log In</a>
        <a href="#">Sign Up</a>
        <a href="#">About</a>
        <a href="#">Pricing</a>
    </nav>
  )
}

export default Navbar